apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: browser-node-cluster-role-binding
subjects:
  - kind: ServiceAccount
    namespace: <GridNamespace>
    name: browser-node
roleRef:
  kind: ClusterRole
  name: browser-node-role
  apiGroup: rbac.authorization.k8s.io
